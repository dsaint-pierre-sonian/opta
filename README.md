This will probably only work on Macs.

Install phantomjs.

`brew install phantomjs`

To run the script:

`ruby opta_export.rb`

It will export to a csv file.

For help/options, run `./opta_export.rb -h`.
